services:
  frontend:
    image: nginx:1.27-alpine
    container_name: abeypad-frontend
    restart: unless-stopped
    networks:
      - proxy
    volumes:
      - ../dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro

networks:
  proxy:
    external: true

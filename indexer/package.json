{
  "name": "ans-abeychain-subgraph",
  "version": "1.0.0",
  "description": "Goldsky subgraph for the Abeychain Name Service (ANS)",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "codegen:build": "graph codegen && graph build",
    "deploy": "goldsky subgraph deploy ans-abeychain/1.0.0 --path ./build --tag prod",
    "deploy:dry": "echo 'Run: goldsky subgraph deploy ans-abeychain/1.0.0 --path ./build --tag prod'"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.35.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.91.0",
    "typescript": "^5.0.0"
  }
}
